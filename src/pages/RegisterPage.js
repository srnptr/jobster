import React from "react";
import Wrapper from "../assets/styled-wrappers/RegisterPage";
import { Register } from "../components";

const RegisterPage = () => {
  return (
    <Wrapper className="full-page">
      <Register />
    </Wrapper>
  );
};

export default RegisterPage;
